import 'package:flutter/material.dart';

class CurrentPlanTree extends StatelessWidget {
  const CurrentPlanTree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container( child: Text('tree'));
  }
}
